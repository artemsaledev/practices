<?php

<!-- ЗАДАНИЕ 1

- Создайте переменную $name и присвойте ей значение содержащее Ваше имя, например "Владимир"(обязательно в кавычках!)
- Создайте переменную $age и присвойте ей значение содержащее Ваш возраст, например 25

$name 

 
ЗАДАНИЕ 2

- Выведите с помощью echo(или print) фразу "Меня зовут: ваше_имя"
- Выведите фразу "Мне ваш_возраст лет", например: "Мне 25 лет"
- Удалите переменную $age. ()

ЗАДАНИЕ 3

- Создайте переменную $age
- Присвойте переменной $age произвольное числовое значение ( используя функцию rand() в документации прочитать что это за функция)
Повторяем теорию и делаем практические задания:

Условные операторы PHP:
1) if
2) else
3) Elseif

Конструкции выбора:
•	switch-case

Смешанные типы данных:
1) Массивы (Array)

Циклы:
•	Циклы:

 
Задачи:

•	В начале файла в php-блоке напишите: 

/* Получаем текущий час в виде строки от 00 до 23 и приводим строку к целому числу от 0 до 23 */

$hour = (int)strftime('%H');- чтобы не ждать можно использовать функцию rand(минимальное значение, максимальное значение);

$welcome = '';

Инициализируем переменную для приветствия
Используя управляющую конструкцию if – elseif - else присвойте переменной $welcome значение, исходя из следующих условий если число в переменной $hour попадает в диапазон:

от 0 до 6 - Доброй ночи 
от 6 (включительно) до 12 - Доброе утро 
от 12 (включительно) до 18 - Добрый день 
от 18 (включительно) до 23 - Добрый вечер 

Если число в переменной $hour не попадает ни в один из вышеперечисленных диапазонов, то присвойте переменной $welcome значение Доброй ночи --> 

 
<!-- 2)Создать массив из 100 случайных чисел.(дальше работаем с ним). Посчитать количество элементов массива , не используя count. Отфильтровать массив так чтобы четные числа записались в 1 масив нечетные в другой. -->

<!-- 3) Вывести числа в следующей последовательности: 200 199 198 … 0. Задание решите с помощью цикла do…while -->

<!-- 4)Найти сумму 1+4+7+10+...+112. Ответ: 2147 -->

<!-- 5)С помощью цикла напечатайте таблицу умножения на 7 от 1 до 9.

Т.е.:
1*7=7
2*7=14
3*7=21… -->

<!-- 6) Создать ассоциативный массив из 10 элементов. Вывести все его ключи. Перебрать массив так чтобы если в значении было число мы заменяем его на “тут было число n(где n наше число)” , если строка “Здесь была строка из n символов”.
?> -->